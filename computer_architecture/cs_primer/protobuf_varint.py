import struct

def main():
    
   

def convert_to_bit_bus(hex):



def encode(num):
    # first we need to get hex values of number
    values = struct('<h', num)

    # seperate 7 bits at a time #

    # insert continuation bit 

        # if it's the MSB add one - if not add 0
        # also if there is one bit left over add 1 to second byte 

    # turn bits into a hex number

    # package to return value 
    

if __name__ == "__main__":
    main()