#include <stdio.h>

void escape(char s[], char t[]);

int main(void)
{

}

void escape(char s[], char t[])
{
  int c, i; 

  while ((c = s[i]) != EOF) {
    switch (c) {
      case '\n': case '\t':
        
        break;
    }
  }
}
